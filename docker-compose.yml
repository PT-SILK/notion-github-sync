version: "3.9"

services:
  notion-github-sync:
    build: .
    container_name: notion-github-sync
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - ./logs:/app/logs
